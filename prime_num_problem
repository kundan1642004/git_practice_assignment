// it is a prime or not 

let num=23;
let count_factor=0;

for (let i = 0; i <= num; i++) {
  if (num % i== 0) {
    count_factor++;
  }
}

if (count_factor == 2) {
  console.log(num, "is prime number");
}
else {
  console.log(num, "is not prime");
}